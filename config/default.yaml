listen:
  port: 9000

webserver:
  https: false
  hostname: 'localhost'
  port: 9000

database:
  hostname: 'localhost'
  port: 27017
  suffix: '-dev'

# From the project root directory
storage:
  certs: 'certs/'
  videos: 'videos/'
  logs: 'logs/'
  thumbnails: 'thumbnails/'
  torrents: 'torrents/'
