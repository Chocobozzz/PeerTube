'use strict'

var reqValidators = {
  videos: require('./videos'),
  pods: require('./pods'),
  remote: require('./remote')
}

// ---------------------------------------------------------------------------

module.exports = reqValidators
